"""
storage/ — capa de persistencia de la plataforma.

Módulos:
  storage.postgres  — pool ThreadedConnectionPool con retry
  storage.redis     — cliente Redis singleton
  storage.minio     — cliente MinIO para objetos
"""
